# NetWatch 1.4.0 验证记录

验证日期：2026-09-24。环境：本机 Windows、.NET Framework C# 编译器、浏览器。

## 自动化检查

核心/SNMP 测试 38 项通过，HTTP 集成测试 27 项通过，共 65 项。

核心测试使用本机 UDP SNMP 应答器，发送和接收标准 SNMP v2c 消息。验证了系统 GET、GETBULK 端口表、64 位计数器、两次采样约 10 Mbps 流量、32 位安全回绕与多次回绕拒绝、设备重启、接口 discontinuity、SNMP 不响应但 Ping 可达的状态分类，以及 H3C/华为 CPU/内存自动探测。新增回归覆盖了首个实体为 0 时跳过无效行、主实体 5%/其他板卡 70% 时不取最大值，以及标准系统 RAM 优先于实体内存。

同时验证了 Windows DPAPI 加解密、配置中没有明文社区字符串、公开接口不返回密文、连续三次失败告警、确认和恢复区别、端口/错误包/CPU 告警及恢复、编辑保留社区字符串、历史保存与重启恢复。

HTTP 测试验证了普通用户启动、嵌入网页资源、局域网访问地址发布、会话校验、Host/Origin 校验、演示数据标记、详情和 CSV、设备新增编辑删除、重复地址和非法地址拒绝、社区字符串脱敏、总览历史与接口数据，以及拓扑位置保存、读取、边界校验和重置。

## 浏览器检查

- 三栏网络运维大屏正常显示设备总览、类型分布、网络质量、流量趋势、TOP 5、中央全网态势、链路状态、告警、设备状态、CPU/内存趋势和实时事件。
- 网络拓扑节点、拖动提示、布局重置入口和局域网访问地址正常显示；拖动位置自动写入数据目录的 `topology.json`，重启仍可恢复。
- 20 台设备的拓扑实际浏览器检查中，20 个节点均完整位于当前画布边界内，页面无需纵向滚动或先拖动节点。
- 14 台浏览器测试设备按名称排序；第一页 10 台、第二页 4 台，上一页/下一页禁用状态和页码正确。三台明确标注的演示设备、设备详情、8 个端口、流量图表正常显示。
- 添加设备、暂停定时监测、编辑名称并保留社区字符串完成实操验证。
- 窄屏布局和 1440 像素桌面布局已检查；接口表支持横向滚动。
- 详情页存在一个历史图表和 8 行端口数据；浏览器日志无 JavaScript 错误或 CSP 警告。

## 验证范围

测试没有扫描用户的局域网，没有连接或修改真实交换机、路由器、防火墙。未进行真实厂商兼容性、长期稳定性或 100 台满负载测试。SNMP 自动指标仍需在真实设备上核对 MIB 视图权限与实体索引口径。

源码包内提供 Test.ps1、Tests.cs 与 http-test.js，可复现自动化验证。测试数据不会打包进交付物。
