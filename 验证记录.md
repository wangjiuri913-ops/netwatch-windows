# NetWatch 1.0 验证记录

验证日期：2026-09-23。环境：本机 Windows、.NET Framework C# 编译器、浏览器。

## 自动化检查

核心/SNMP 测试 28 项通过，HTTP 集成测试 19 项通过，共 47 项。

核心测试使用本机 UDP SNMP 应答器，发送和接收标准 SNMP v2c 消息。验证了系统 GET、GETBULK 端口表、64 位计数器、两次采样约 10 Mbps 流量、32 位安全回绕与多次回绕拒绝、设备重启、接口 discontinuity、SNMP 不响应但 Ping 可达的状态分类，以及 CPU/内存自定义 OID。

同时验证了 Windows DPAPI 加解密、配置中没有明文社区字符串、公开接口不返回密文、连续三次失败告警、确认和恢复区别、端口/错误包/CPU 告警及恢复、编辑保留社区字符串、历史保存与重启恢复。

HTTP 测试验证了普通用户启动、嵌入网页资源、会话校验、Host/Origin 校验、演示数据标记、详情和 CSV、设备新增编辑删除、重复地址和非法地址拒绝、社区字符串脱敏。

## 浏览器检查

- 总览、三台明确标注的演示设备、设备详情、8 个端口、流量图表正常显示。
- 添加设备、暂停定时监测、编辑名称并保留社区字符串完成实操验证。
- 窄屏布局和 1440 像素桌面布局已检查；接口表支持横向滚动。
- 详情页存在一个历史图表和 8 行端口数据；浏览器日志无 JavaScript 错误或 CSP 警告。

## 验证范围

测试没有扫描用户的局域网，没有连接或修改真实交换机、路由器、防火墙。未进行真实厂商兼容性、长期稳定性或 100 台满负载测试。CPU/内存 OID 需要按真实设备型号配置。

源码包内提供 Test.ps1、Tests.cs 与 http-test.js，可复现自动化验证。测试数据不会打包进交付物。
