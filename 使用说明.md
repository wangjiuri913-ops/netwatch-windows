# NetWatch 网络监测平台 · Windows 便携版 1.0

用于在一台 Windows 电脑上监测支持 SNMP v2c 的交换机、路由器、防火墙。浏览器是管理界面，后台程序负责轮询。程序只发送 SNMP GET/GETBULK 和 ICMP 探测，不向设备写配置。

## 快速开始

1. 将 ZIP **完整解压**到一个可写目录，例如 `D:\NetWatch`。不要直接从压缩包内部运行 EXE，也不要单独移动 EXE 而漏掉 DLL。
2. 推荐双击 `Start-Portable.cmd`：启动程序和浏览器，配置、历史存放在旁边的 `Data` 文件夹。放在 D 盘即可避免把历史写到 C 盘。
3. 浏览器打开 `http://127.0.0.1:18765`。点击“添加设备”，填写设备管理 IP 和 SNMP v2c 只读社区字符串。
4. 默认每 60 秒采集一次。首次采集显示状态和接口；第二次成功采集开始计算流量。也可点击“立即采集”。
5. 点击设备名称或“详情”，查看端口、流量曲线和历史 CSV。

要先看效果，可双击 `Start-Demo.cmd`：使用端口 18767 和独立 `DemoData` 目录，加载三台模拟设备。界面会明确标注演示数据，不连接真实交换机。

直接双击 `NetWatch.exe` 也可以启动，此方式将数据保存在 `%LOCALAPPDATA%\NetWatch`。不要把两种启动方式的不同数据目录误认为数据丢失；同一端口不能同时运行两个实例。

## 运行环境

- Windows 10 / 11，安装 .NET Framework 4.7.2 或更高版本（新版本 Windows 通常已有 4.8）。.NET 8 与 .NET Framework 是不同运行环境。
- 建议用新版 Edge 或 Chrome。正常运行无需 Python、Node.js、Docker、数据库服务器或管理员权限。
- 监测电脑到设备的管理 IP 需要可达；设备端允许 SNMP UDP 端口（通常 161）。ICMP 被禁止时仍可通过 SNMP 采集。
- 电脑必须保持开机。关闭浏览器不会停止采集；电脑睡眠期间不会采集，也不会补回缺失数据。
- 停止程序：右键任务栏通知区域的 NetWatch 图标 →“退出并停止监测”。程序不自动设置开机启动或安装系统服务。

## 支持的数据与操作

| 项目 | 功能 |
| --- | --- |
| 设备管理 | 新增、编辑、移除、暂停、按类型和关键词筛选 |
| 在线状态 | SNMP 响应、Ping 可达但 SNMP 异常、两者未响应 |
| 延迟与探测无响应率 | 每轮 3 次 ICMP；延迟为成功响应的平均值，无响应率以 3 次探测计算 |
| 系统信息 | 系统名称、描述、sysUpTime 运行时间 |
| 接口信息 | 名称、描述、管理状态、运行状态、接口速率 |
| 流量 | 每接口入/出 bps、利用率、设备全部接口合计 |
| 错误与丢弃 | 接口累计入/出错误包、入/出丢弃包计数 |
| CPU / 内存 | 支持用户填写返回 0–100 百分比的数字 OID，不自动猜测厂商 OID |
| 历史 | 设备接口合计流量、延迟、无响应率、CPU/内存、最大利用率；最近 24 小时曲线与 CSV 导出 |
| 告警 | 连续失败、恢复、端口 UP 变非 UP、错误/丢弃计数增加、带宽/CPU/内存阈值；可确认 |

本版历史是**设备所有接口的合计**，接口表显示当前采样；未保存每个接口的独立历史曲线。

## 设备侧怎样接入

在设备管理界面或配置中启用 SNMP v2c，配置**只读**社区字符串，并在 SNMP 访问控制列表中允许监测电脑的管理 IP。至少开放 system、IF-MIB / IF-X-MIB 的读取权限。厂商和型号的命令不同，请按对应设备手册配置。

SNMP v2c 的社区字符串在线路上不加密，应使用受控管理网络。当前版本没有 SNMP v3；不要把只允许 v3 的设备强行改成 v2c 接入不受控网络。

CPU/内存：在“编辑设备 → 可选 OID”填写厂商文档中的百分比对象，必须包含实例索引（标量通常以 `.0` 结尾）。返回字节数、字符串描述、多个 CPU 表行或需要除以 100 的数值不能直接作为百分比。未填写或不支持时显示无数据。

### 通用 OID 参考

| 对象 | OID |
| --- | --- |
| sysDescr.0 | 1.3.6.1.2.1.1.1.0 |
| sysUpTime.0 | 1.3.6.1.2.1.1.3.0 |
| sysName.0 | 1.3.6.1.2.1.1.5.0 |
| ifTable | 1.3.6.1.2.1.2.2 |
| ifXTable | 1.3.6.1.2.1.31.1.1 |
| ifHCInOctets | 1.3.6.1.2.1.31.1.1.1.6.ifIndex |
| ifHCOutOctets | 1.3.6.1.2.1.31.1.1.1.10.ifIndex |
| ifHighSpeed | 1.3.6.1.2.1.31.1.1.1.15.ifIndex |
| ifCounterDiscontinuityTime | 1.3.6.1.2.1.31.1.1.1.19.ifIndex |

## 怎样理解数据

- 流量 = 相邻计数器差值 × 8 ÷ 实际采样间隔，单位为 bit/s；不是文件下载的 Byte/s。
- 利用率 = 入/出方向较高值 ÷ 接口速率。接口速率未知时不显示利用率。
- 优先使用 64 位计数器。32 位计数器在高速接口上可能在一个采样周期内多次回绕；此时程序显示“—”，不猜测流量。
- 首次采样、设备运行计数减少、接口计数器重置、接口名称/速率变化、过旧基线或链路非 UP 时，会等待新的有效基线。
- 接口合计可能重复计入转发、VLAN、聚合口和物理口，不能当作出口互联网带宽。查看对应接口更有意义。
- ICMP 无响应不等于业务丢包率，也不一定意味着设备断电；设备可能禁 Ping 或限速。页面会区分 SNMP 和 ICMP 状态。
- 告警默认阈值为 85%，恢复使用约 5 个百分点的滞回；确认告警只表示已知晓，不会伪造恢复。
- 端口首次就是 DOWN 不会直接告警，只有采集到 UP → 非 UP 的变化才触发；管理关闭的备用口不会因此持续报警。
- 程序启动后重新累计连续失败次数；历史告警与状态会恢复。

## 数据保存与备份

便携启动默认使用程序目录下 `Data`；普通 EXE 启动使用 `%LOCALAPPDATA%\NetWatch`。

- `devices.json`：设备配置；社区字符串使用 Windows DPAPI 按当前用户加密。
- `alerts.json`：最多保存 2000 条告警，界面展示最近 250 条。
- `<设备ID>.json`：当前状态及采集时滚动保留的 24 小时历史，每设备最多 2880 个点。
- `.bak`：最近一次保存前的备份；写入采用临时文件和原子替换。
- `netwatch.log`：启动和异常日志，滚动保留少量日志。

备份前先退出程序，再复制整个数据目录。换电脑或换 Windows 用户后，社区字符串不能直接解密，需要在设备编辑页重新填写。移除设备会停止监测，磁盘中的历史文件保留；正式需要恢复设备时可在退出后从配置备份恢复，注意备份时间可能包含其他变更。

历史文件按设备数增长。存储空间不足时，页面会提示保存失败，当前内存中的数据仍可查看，但退出可能丢失未保存部分。

## 本版范围

- 上限为 100 台设备、每设备 512 个接口、4 台并发轮询；设备数量多或 SNMP 响应慢时，实际轮询可能晚于配置时间。
- 轮询间隔可设为 30–3600 秒，每请求超时 300–5000 毫秒；一次 SNMP 采集有 30 秒预算。
- 界面只监听 `127.0.0.1`，没有多人登录和局域网远程访问。
- 不包含 SNMP v3、Trap/Syslog、NetFlow/sFlow、自动拓扑发现、厂商私有会话/VPN/温度指标模板、邮件/短信通知或 Windows 系统服务。
- 软件已经过本机模拟 SNMP 设备测试和浏览器测试，尚未连接你的真实网络设备；不同厂商兼容性应在接入第一台设备后核对。

## 启动参数与开发

```text
NetWatch.exe
NetWatch.exe --data="D:\NetWatchData"
NetWatch.exe --port=18888 --data="D:\NetWatchData"
NetWatch.exe --demo --port=18767 --data="D:\NetWatchDemo"
```

`--headless` 用于自动化测试，无托盘和浏览器，不建议日常双击使用。

源码在随附 `source` 目录中，含 SharpSnmpLib 12.5.7、完整前后端及测试。构建用 Windows PowerShell 执行 `source\Build.ps1`，产物默认位于 `source\bin`。源码构建无需联网或 .NET SDK，使用系统 .NET Framework C# 编译器。

`source\Test.ps1` 运行 28 项核心/SNMP 测试；安装 Node.js 18+ 的开发环境还可运行 `node source\http-test.js` 执行 19 项 HTTP 集成测试（先构建）。测试使用独立临时目录和回环地址，不扫描真实网络。

第三方许可见 `THIRD-PARTY-NOTICES.txt`。
